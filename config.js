/* Bachata Library configuration.
 * GOOGLE_CLIENT_ID: an OAuth 2.0 client ID of type "Web application" from the
 * Google Cloud console. It is public by design (there is no secret); Google only
 * issues tokens for it to the origins registered on that client.
 * Leave empty to enter it in the app's setup screen instead.
 */
window.BACHATA_CONFIG = {
  GOOGLE_CLIENT_ID: ""
};
